import API from "../api/api";
await API.post("orders", {

    customer,

    items,

    totalAmount,

    deliveryType,

    paymentMethod,

    transactionId,

     paymentScreenshot: req.file
    ? `uploads/${req.file.filename}`
    : "",

});

await order.save();
